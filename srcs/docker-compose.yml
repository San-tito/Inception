services:
  nginx:
    build: ./requirements/nginx
    ports:
      - "8080:80"
    restart: on-failure

  wordpress:
    build: ./requirements/wordpress
    restart: on-failure

  mariadb:
    build: ./requirements/mariadb
    restart: on-failure

networks:
  default:
    driver: bridge
